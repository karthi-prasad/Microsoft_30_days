#include <bits/stdc++.h>
using namespace std;

void arrangementSeat(int n) // n(th) seat
{
    // the person sits on order based on the ticket return 1
    // otherwise return 0.5 (1/n)
    /*if (n == 1)
    {
        cout << "Passenger seats based on their tickets" << endl;
        cout << "1" << endl;
    }
    else
    {
        cout << "Passengers seats based on random seats" << endl;
        cout << "0.5" << endl;
    }*/
    // leetcode submission
    if (n == 1) ? return 1 : return 0.5;
}